# داکیومنت جامع وضعیت پروژه و نقشه راه فنی (Handoff & Execution Blueprint)
**پروژه:** COOL Gallery Shop (`cool-gallery-shop`)  
**تاریخ و نسخه:** ۲۳ سپتامبر ۲۰۲۶ — نسخه انتقال و ادامه کار  
**مخاطب:** ایجنت هوش مصنوعی / توسعه‌دهنده بعدی جهت ادامه بدون وقفه  

---

## ۱. اهداف اصلی کاربر و بستر پروژه (Context & Objectives)

### هدف و خواسته کاربر:
1. **تمایز کامل از پروژه `avval-frontend`:** کاربر اعلام کرد که انیمیشن‌ها و لندینگ پیج این پروژه خیلی شبیه به پروژه `avval-frontend` شده و خواستار یک **طراحی انیمیشن کاملاً متفاوت، سینماتیک‌تر، لوکس‌تر و جذاب‌تر برای پرزنت محصولات** شد.
2. **اصلاح و ارتقای کیفیت تصاویر محصولات:** تصاویر محصولات قبلی بی‌کیفیت و با پس‌زمینه نامناسب بودند که نیاز به تصاویر تر و تمیز، Full HD و ترنسپرنت (PNG) دارند.
3. **رفع تداخلات استایل و هدر:** تایتل‌ها و المان‌ها نباید زیر هدر شیشه‌ای بروند (فاصله‌گذاری صحیح).

### مشخصات فنی پروژه:
- **فریم‌ورک:** Next.js 16.3.4 (App Router, Turbopack) + React 19.2.8
- **استایل‌دهی:** Tailwind CSS v4 + `tw-animate-css` + کلاس‌های Glassmorphism اختصاصی
- **کتابخانه انیمیشن:** `motion/react` (نسخه ۱۳ به بالا — **توجه:** از `framer-motion` ایمپورت نکنید؛ از `"motion/react"` استفاده شود)
- **زبان و دایرکشن:** فارسی (`RTL`) با فونت‌های Vazirmatn و Plus Jakarta Sans و Bowlby One
- **صفحات هوم:**
  - `app/page.tsx`: صفحه اصلی استاتیک/پایه
  - `app/home-test/page.tsx`: صفحه اصلی آزمایشی با انیمیشن‌های سینماتیک پیشرفته (تمرکز اصلی کاربر روی این صفحه است)

---

## ۲. گزارش تحلیل مقایسه‌ای با `avval-frontend` (علت شباهت قبلی و راهکار تمایز)

پروژه `avval-frontend` واقع در مسیر `/Users/sy313/projects/avval-frontend` به طور کامل تحلیل شد. الگوهای مشترکی که باعث شباهت شده بودند و نحوه جایگزینی آن‌ها در جدول زیر مشخص شده است:

| بخش / الگو | الگوی قبلی (مشابه avval-frontend) | الگوی جدید پیاده‌سازی شده (Cinematic Runway) | وضعیت |
| :--- | :--- | :--- | :--- |
| **نمایش محصول در دسکتاپ** | استیج چرخشی و اوربیتال / Sticky Plans | **Theatrical Split-Screen Reveal** با پرده‌های ClipPath Wipe و پارالاکس مگنتیک ماوس | ✅ پیاده شد |
| **نمایش مجموعه‌ها** | کارت‌های چسبان آبشاری (Folder Sticky Cards) | **Horizontal Runway 3D Gallery** با تبدیل اسکرول عمودی به افقی و چرخش `rotateY` سه‌بعدی | ✅ پیاده شد |
| **نورپردازی پس‌زمینه** | گوی‌های گرادیان شناور (Floating Gradient Orbs) | **Cinematic Spotlight Sweep** + نور متمرکز پروژکتوری + افکت CRT Scanline | ✅ پیاده شد |
| **تایپوگرافی و شمارنده‌ها** | Fade-in ساده و استاتیک | واترمارک بزرگ فریم سینمایی (`FRAME 01/04`) با فونت دیجیتال `cinematic-counter` | ✅ پیاده شد |

---

## ۳. وضعیت کارهای انجام شده تا این لحظه (Completed Work)

### الف) استایل‌ها و کی‌فریم‌های سینماتیک (`app/globals.css`):
- کی‌فریم `@keyframes spotlight-sweep`: حرکت زاویه‌دار نور پروژکتور روی صحنه
- کی‌فریم `@keyframes film-flicker`: شبیه‌سازی لرزش فریم نوار فیلم آپارات
- کی‌فریم `@keyframes scanline-scroll`: خطوط اسکن CRT
- کی‌فریم `@keyframes divider-pulse`: پالس نوری خط جداکننده وسط پرده
- کلاس‌های یوتیلیتی: `.film-strip-border` (سوراخ‌های پرفوراسیون نوار فیلم ۳۵ میلی‌متری)، `.scanline-overlay`، `.cinematic-counter`

### ب) بازنویسی کامپوننت Split-Screen دسکتاپ (`components/home-test/desktop-wheel-showcase.tsx`):
- حذف کامل چرخ گردان و منظومه شمسی
- تقسیم صفحه به دو نیمه متقارن (چپ: استیج تصویر محصول با نور اختصاصی / راست: تایتل، توضیحات، مشخصات، قیمت و دکمه خرید)
- پارالاکس فیزیکی ماوس با Spring Physics روی عکس محصول هنگام حرکت موس کاربر
- ترنزیشن محصولات با انیمیشن `clipPath: inset(...)` شبیه به تعویض کات‌های فیلم سینمایی
- نمایش شماره فریم عظیم در پس‌زمینه (`01`, `02`, ...)
- خط نورانی لیزری در خط فاصل مرکزی (`animate-divider-pulse`)

### ج) بازنویسی گالری تازه‌ها به ران‌وی افقی سه‌بعدی (`components/home-test/sticky-cards-showcase.tsx`):
- تبدیل اسکرول عمودی به اسکرول روان افقی (Scroll-jacking با `useSpring`)
- پرسپکتیو سه‌بعدی (`perspective: 1200px`) و چرخش زاویه‌ای کارت‌ها (`rotateY`) براساس فاصله از مرکز
- نوارهای پرفوراسیون نگاتیو فیلم در بالا و پایین صفحه (`film-strip-border`)
- شمارنده فریم زنده و تغییر ابعاد/شفافیت کارت‌ها هنگام رد شدن از مرکز

### د) بازطراحی بخش تخفیف و پروموشن (`components/home-test/promo-section.tsx`):
- حذف تمام اورب‌های شناور گرادیانتی
- اعمال افکت اسکن‌لاین CRT روی پنل شیشه‌ای
- افکت نئون چشمک‌زن واقعی (`animate-neon-flicker`) برای عدد ۳۰٪ همراه با رفلکس نوری زیر آن

### ه) تصاویر محصولات (`public/products/` و `lib/data.ts`):
- ۱۰ تصویر هوش مصنوعی ساخته شد و با اسکریپت آلفا متینگ به PNG تبدیل شدند.

### و) اعتبارسنجی کد:
- دستور `npx tsc --noEmit` با موفقیت و بدون هیچ‌گونه تایپ ارور یا باگ اجرا شد (`Exit Code: 0`).

### ز) هماهنگ‌سازی کامپوننت موبایلی (`components/home-test/product-showcase.tsx`) — ۲۳ سپتامبر ۲۰۲۶:
- رویکرد قدیمی Z-Space Fly-Through (اسکیل تا ۴.۵ برابر + بلور ۳۵px) کاملاً حذف شد.
- جایگزین شد با **Curtain Wipe** عمودی مبتنی بر `clip-path: inset(...)` که پیوسته به اسکرول لمسی متصل است (نه AnimatePresence گسسته) — جهت مناسب برای اسکرول عمودی موبایل، هم‌خانواده با wipe افقی دسکتاپ.
- نوارهای پرفوراسیون فیلم (`film-strip-border`)، اسکن‌لاین CRT (`scanline-overlay`)، لرزش نوار آپارات (`animate-film-flicker`) و واترمارک شماره فریم (`cinematic-counter`, `FRAME 0X/0Y`) به کامپوننت اضافه شد.
- پالت نورپردازی (`THEMES`) دقیقاً با نسخه دسکتاپ (`desktop-wheel-showcase.tsx`) یکسان شد تا هویت بصری بین دو بریک‌پوینت یکپارچه باشد.
- تست بصری در Browser pane (موبایل ۳۷۵×۸۱۲) روی هر ۴ محصول انجام و تأیید شد.

### ح) رفع باگ مشترک انیمیشن `film-flicker` (`app/globals.css`):
- کی‌فریم `film-flicker` قبلاً بین opacity حدود ۰.۹ تا ۱ نوسان می‌کرد، در حالی‌که همیشه روی المانی با کلاس `opacity-[0.03]` اعمال می‌شد. چون انیمیشن CSS همیشه روی propertyی که انیمیت می‌کند غالب می‌شود، این تداخل باعث می‌شد لایه سفید تقریباً کاملاً غیرشفاف (به‌جای ۳٪) کل صفحه را بپوشاند.
- این باگ در بخش موبایل تازه‌ساخته‌شده کشف شد، اما همان کلاس در `sticky-cards-showcase.tsx` (بخش «تازه‌های گالری») هم استفاده شده بود و به همان مشکل دچار بود.
- رفع شد با تغییر مقادیر کی‌فریم به بازه‌ی ۰.۰۱۵ تا ۰.۰۳ (هم‌راستا با یوتیلیتی استاتیک) — رفع در سطح `globals.css` هر دو مصرف‌کننده را همزمان اصلاح کرد.

### ط) بازطراحی و رفع باگ اسکرول ران‌وی «تازه‌های گالری» (`components/home-test/sticky-cards-showcase.tsx`) — ۲۳ سپتامبر ۲۰۲۶:
- **گزارش کاربر:** «این بخش که اسکرولش برعکسه، هر چی اسکرول می‌کنم از کادر خارج‌تر می‌شه» + «دیزاینش خیلی پاورپوینتی شده».
- **باگ ۱ (اصلی):** `translateX` با درصد (`-${(n-1)*100}%`) نسبت به عرض خودِ ریل محاسبه می‌شود، نه یک کارت. چون عرض ریل = مجموع هر ۴ کارت بود، مقدار نهایی چند برابر فاصله واقعی لازم بود و کارت‌ها کاملاً از صفحه خارج می‌شدند. **راه‌حل:** اندازه‌گیری واقعی عرض ریل با `ref.scrollWidth` در `useEffect` و محاسبه‌ی `maxTranslate` بر حسب پیکسل، سپس `useTransform(progress, [0,1], [0, -maxTranslate])`.
- **باگ ۲ (ظریف‌تر، کشف‌شده حین تست):** حتی بعد از رفع باگ ۱، چون سایت `dir="rtl"` است، یک فرزند flex عریض‌تر از والدش به‌طور پیش‌فرض به راست لنگر می‌شود و بدون هیچ اسکرولی به چپ overflow می‌کند — این offset استاتیک با `translateX` محاسبه‌شده جمع می‌شد. **راه‌حل:** `dir="ltr"` روی خودِ container سیستیکی (والد flex که ریل را نگه می‌دارد) تا لنگر پیش‌فرض چپ شود و پایه صفر پیش‌بینی‌پذیر باشد؛ هر کارت به‌صورت جداگانه `dir="rtl"` می‌گیرد تا متن فارسی داخلش درست چیده شود.
- ⚠️ **نکته برای ایجنت بعدی:** اگر جای دیگری از پروژه (مثلاً `desktop-wheel-showcase.tsx` یا کامپوننت‌های آینده) از `motion`/`transform` روی یک ردیف افقیِ عریض‌تر از container در بستر RTL استفاده شد، همین الگوی دو-باگی را چک کنید.
- **بازطراحی بصری** (پاسخ به «پاورپوینتی»): حذف واترمارک غول‌پیکر شماره فریم، حذف بج‌های چانکی مشخصات، حذف نوار پرفوراسیون فیلم — جایگزین با تایپوگرافی باز و مینیمال (`N.01 — دسته‌بندی`)، فضای منفی بیشتر دور تصویر، دکمه CTA به‌صورت لینک متنی ظریف به‌جای دکمه سفید حجیم، و یک نوار پیشرفت نازک به‌جای شمارنده/نقطه چانکی. Tilt سه‌بعدی (`rotateY`) ملایم‌تر شد (۱۸ درجه به‌جای ۳۵).
- تست بصری کامل در Browser pane روی دسکتاپ (۱۰۲۴×۷۶۸) و موبایل (۳۷۵×۸۱۲) از ابتدا تا انتهای رنج اسکرول انجام شد — دیگر هیچ کارتی از کادر خارج نمی‌شود.

---

## ۴. لیمیت‌ها، چالش‌ها و نکات فنی مهم (Limitations & Warnings)

> [!WARNING]
> **۱. محدودیت ساخت تصویر هوش مصنوعی (Image Generation Quota):**
> سهمیه ابزار `generate_image` برای چند ساعت لیمیت شده است. از فایل‌های موجود در `public/products/` استفاده کنید یا در صورت نیاز به روتوش پس‌زمینه، از پردازش پیکسلی مستقیم استفاده شود.

> [!IMPORTANT]
> **۲. محدودیت بیلد تستی سندباکس (`npm run build`):**
> دستور `npm run build` در محیط سندباکس به دلیل عدم دسترسی به اینترنت خارجی برای دانلود گوگل فونت‌ها خطا می‌دهد. **معیار تست سلامت کد فقط دستور `npx tsc --noEmit` است.**

> [!NOTE]
> **۳. نسخه کتابخانه موشن (`motion/react`):**
> همواره باید از `import { motion, ... } from "motion/react"` استفاده شود و آبجکت‌های Variants صریحاً تایپ `Variants` دریافت کنند تا تداخل تایپ‌اسکریپت با `AnimationGeneratorType` پیش نیاید.

---

## ۵. نقشه راه و وظایف بعدی برای ایجنت بعدی (Step-by-Step Next Steps)

اگر ایجنت بعدی این وظیفه را دریافت کند، باید این گام‌ها را طی کند:

### گام اول: همگام‌سازی بخش موبایل (`components/home-test/product-showcase.tsx`) — ✅ انجام شد (۲۳ سپتامبر ۲۰۲۶)
- کامپوننت موبایلی با Curtain Wipe، نوار فیلم، اسکن‌لاین و واترمارک فریم هماهنگ شد. جزئیات در بخش ۳-ز و ۳-ح بالا.

### گام دوم: بهبود کیفیت بصری و برش پس‌زمینه تصاویر محصولات
- کاربر قبلاً اشاره کرده بود که برش تصاویر و پس‌زمینه آنها نیاز به تمیزی بیشتر دارد.
- بررسی تصاویر موجود در `/public/products/` و اطمینان از شفافیت بی‌نقص لبه‌ها بدون هاله دایره‌ای مات.

### گام سوم: اعمال اختیاری تم جدید به صفحه اصلی اصلی (`app/page.tsx`)
- در حال حاضر تغییرات روی صفحه آزمایشی `/home-test` (واقع در `app/home-test/page.tsx`) اعمال شده است.
- پس از تایید نهایی کاربر، این کامپوننت‌های مدرن می‌توانند جایگزین بخش‌های متناظر در صفحه اصلی اصلی (`app/page.tsx`) شوند.

---

## ۶. ساختار فایل‌های کلیدی تغییر یافته

```
cool-gallery-shop/
├── app/
│   ├── globals.css                       <-- کی‌فریم‌های سینماتیک، scanline، film-strip
│   ├── page.tsx                          <-- صفحه اصلی پروداکشن
│   └── home-test/
│       └── page.tsx                      <-- صفحه فعال تحت توسعه با انیمیشن‌های جدید
├── components/
│   └── home-test/
│       ├── desktop-wheel-showcase.tsx    <-- [جدید] Theatrical Split-Screen Reveal
│       ├── sticky-cards-showcase.tsx     <-- [جدید] Horizontal 3D Runway Gallery
│       ├── promo-section.tsx             <-- [جدید] Neon + CRT Scanline Promo
│       ├── hero-section.tsx              <-- هیرو اصلی با نورپردازی
│       └── product-showcase.tsx          <-- کامپوننت نمایش موبایلی
├── lib/
│   └── data.ts                           <-- دیتا، دسته‌بندی‌ها و متادیتاهای محصولات
└── public/
    └── products/                         <-- تصاویر PNG محصولات
```

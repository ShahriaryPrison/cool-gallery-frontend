"use client";

/** True immediately so the page and hero render without delay. */
export function useIntroDone() {
  return true;
}

/**
 * True immediately so the hero's logo renders without delay.
 */
export function useIntroLanded() {
  return true;
}
